export enum Role {
    REPORTER = 'REPORTER',
    NGO = 'NGO',
    DONOR = 'DONOR',
    ADMIN = 'ADMIN',
}

export enum CaseUrgency {
    CRITICAL = 'CRITICAL',
    MODERATE = 'MODERATE',
    STABLE = 'STABLE',
}

export enum CaseStatus {
    OPEN = 'OPEN',
    IN_PROGRESS = 'IN_PROGRESS',
    RESOLVED = 'RESOLVED',
}

export enum MedicineStatus {
    AVAILABLE = 'AVAILABLE',
    REQUESTED = 'REQUESTED',
    CLAIMED = 'CLAIMED',
}
